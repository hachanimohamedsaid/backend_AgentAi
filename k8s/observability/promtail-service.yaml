apiVersion: v1
kind: Service
metadata:
  name: promtail
  namespace: pidevagentai
  labels:
    app: promtail
spec:
  selector:
    app: promtail
  ports:
    - name: http
      port: 9080
      targetPort: http
